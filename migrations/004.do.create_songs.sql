CREATE TABLE IF NOT EXISTS songs (
    song_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    song_name TEXT NOT NULL,
    album INTEGER REFERENCES albums(album_id) ON DELETE CASCADE NOT NULL
);