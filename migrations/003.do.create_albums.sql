CREATE TABLE IF NOT EXISTS albums (
    album_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    album_name TEXT NOT NULL,
    genre TEXT NOT NULL,
    artist INTEGER REFERENCES artists(artist_id) ON DELETE CASCADE NOT NULL
);