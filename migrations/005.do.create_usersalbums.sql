CREATE TABLE IF NOT EXISTS usersalbums (
    usersalbums_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER REFERENCES users(user_id) ON DELETE CASCADE NOT NULL,
    album INTEGER REFERENCES albums(album_id) ON DELETE CASCADE NOT NULL
);