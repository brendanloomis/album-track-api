CREATE TABLE IF NOT EXISTS usersartists (
    usersartists_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER REFERENCES users(user_id) ON DELETE CASCADE NOT NULL,
    artist INTEGER REFERENCES artists(artist_id) ON DELETE CASCADE NOT NULL
);